import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  root: {
    width: "100%",
  },
  stepperWidth: {
    maxWidth: "80%",
    margin: "auto",
  },
  styleBtn: {
    margin: theme.spacing(2),
  },
}));
